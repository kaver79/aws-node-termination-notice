apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-node-termination-notice.fullname" . }}
  labels:
    {{- include "aws-node-termination-notice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-node-termination-notice.selectorLabels" . | nindent 4 }}
